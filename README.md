# flowspec-example-skill

A tiny public repository for one flowspec example. It holds one skill
at `.claude/skills/repo-fact/SKILL.md`. The example flow at
`gusleig/flowspec` (`examples/skill-from-repo.flow`) clones this
repository, points a bot's `cwd` at the checkout, and the bot finds
the skill by discovery.
